apiVersion: v1
kind: Secret
metadata:
  name: patient-db-secret
  namespace: healthcare
type: Opaque
# Base64-encoded values — for demo only.
# Production: use Sealed Secrets, Vault, or a cloud KMS.
# Encode:  echo -n "yourvalue" | base64
data:
  mysql-root-password: cm9vdA==      # root
  mysql-database: cGF0aWVudF9kYg==  # patient_db
