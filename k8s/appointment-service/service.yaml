apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: healthcare
  labels:
    app: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  # NodePort exposes the service on each Node's IP at a static port.
  # Use "minikube service appointment-service -n healthcare" to get the URL.
  # Switch to ClusterIP + Ingress for production.
  type: NodePort
