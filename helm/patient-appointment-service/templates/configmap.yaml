apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "patient-appointment-service.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "patient-appointment-service.labels" . | nindent 4 }}
data:
  DB_HOST: {{ include "patient-appointment-service.mysqlServiceName" . }}
  DB_PORT: {{ .Values.mysql.port | quote }}
  DB_NAME: {{ .Values.mysql.database | quote }}
  # Feign client target — must match the K8s Service name of patient-core-service
  PATIENT_SERVICE_URL: {{ .Values.patientService.url | quote }}
